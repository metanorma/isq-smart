# 04-units-page

**Status:** DONE

Created `pages/units.vue` (searchable unit catalog with group-by-part toggle) and `pages/unit.vue` (individual unit detail with linked quantities). Routes: `/units`, `/units/:slug`.
