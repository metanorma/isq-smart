# 14-header-design-parity

**Status:** DONE

Rewrote `Default.vue` layout to match v1 design:
- ISO + IEC logos in header
- Search bar with `/` shortcut
- Domain tabs (Quantities & Units, Math) in desktop nav
- Units, Dims, About, API, Ontology links
- Theme toggle in nav bar
- Mobile menu with search and theme toggle
- SiteFooter, BackToTop, AppToast, GlobalSearch, SearchHint all integrated
