# 08-unitsdb-data

**Status:** DONE

Added units/dimensions data generation to `vite.config.ts` `yamlDataPlugin()`. Generates `src/data/generated/unitsdb.ts` with `units` and `dimensions` exports from quantity entry data.
