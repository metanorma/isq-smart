# 13-global-search

**Status:** DONE

Created `GlobalSearch.vue`, `SearchHint.vue`, and `useSearch.ts` composable.
- Search across all entries with domain filtering (All/Quantities/Math)
- Keyboard navigation (↑↓ Enter Escape), `/` shortcut to open
- Highlight matching text, match field badges
- Dark mode support throughout
