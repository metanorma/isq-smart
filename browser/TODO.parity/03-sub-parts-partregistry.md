# 03-sub-parts-partregistry

**Status:** DONE

Updated `PartRegistry.ts` with all sub-parts (11-4 through 11-9, 2-5 through 2-20) with `parentPart`/`parentTitle` fields. Added `DomainInfo` type and `getDomains`/`getDomain` functions.
