# 12 — Apply Reusable Components

## Problem
Breadcrumb.astro and SectionHead.astro exist but are used by 0 pages.
10 pages have inline breadcrumb markup. Multiple pages have inline section heads.

## Tasks
- [ ] Apply `Breadcrumb.astro` to all pages that have breadcrumb navigation
- [ ] Apply `SectionHead.astro` to all pages with eyebrow+h2 headings
- [ ] Verify build and visual parity
