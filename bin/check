#!/usr/bin/env bash
set -euo pipefail
root="$(cd "$(dirname "$0")/.." && pwd)"
echo "=== Ruby specs ==="
cd "$root"
bundle exec rspec "$@"
echo ""
echo "=== Frontend type check ==="
cd "$root/browser"
npx vue-tsc --noEmit
